1. let items = [];
let arr = new Array();
2. Шкаф с вещами - это массив;
товары в корзине;
фотоальбом;
3. просто использовать delete arr[1];
4. В for все части являются не обязательными. Если нет условия, то цикл выполняется бесконечно, инициализировать, можно  и до цикла
5. while (условие) {

};
let i = 0;
while (i < 3) { 
  alert( i );
  i++;
}

let a = 1;
let b = 10;
while (a<b) {
    alert (a,b);
    a++;
}
6. создаст массив с заданной длинной 6 элеменов
7. for (let i = 2; i <= 10; i++) {
  if (i % 2 == 0) {
    alert( i );
  }
}
8. Выведет на экран, а сам в массив добавится функция. Массив по итогу : ['a','b',f]
9. метод forEach, for и for...in
10. const input = document.querySelector('input');
11. проще всего использовать sort();
12. использовать break;
